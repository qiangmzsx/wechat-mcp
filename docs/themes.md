将`converter`目录下的代码只支持aiConverter ，加入apiConverter，使用`theme`目录的类库支持。
需求：
- 在config中可以指定使用api还是ai的converter
- mcp支持指定api还是ai的converter
- 默认使用api的converter
- 重新组织项目架构，符合设计模式，代码简洁优雅。
- 将ai的theme与api的theme统一以来。